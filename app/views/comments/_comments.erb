<h3>Comments</h3>
<% @post.comments.each do |comment| %>
	<div class="well">
		<p><strong><%= comment.username %></strong>: <%= comment.body %></p>
		<%= link_to '[Delete]', [comment.post, comment],
method: :delete,
data: { confirm: '정말로 삭제하실래요?'} %></p>
</div>
<% end %>